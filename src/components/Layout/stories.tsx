import { Meta, StoryObj } from "@storybook/react";

import Layout from "./index";

export default {
  title: "Layout/Layout",
  component: Layout,
  args: {},
} as Meta;

type Story = StoryObj;

export const Default: Story = {
  args: {},
};
